# Carry-Forward Brief: H&H Legacy & Migration Sonnet
**Session:** local_15623c9a-947f-4b17-a823-aa189940f553  
**Status:** Running (87+ assistant turns)  
**Project:** Hansson Hertzell  
**Generated:** 2026-05-16

## What's Being Worked On
Fixing Next.js serverless function issues on the hanssonhertzell.se site — specifically the `outputFileTracingIncludes` configuration that controls which JSON files get bundled into the serverless function at build time.

## Recent Context
The site's AI content (property descriptions, area guides) is stored as JSON files that need to be accessible to serverless functions at runtime. The Vercel/Next.js build pipeline was not tracing/including these JSON files in the lambda bundle, causing the AI content to be missing on the live site.

- Tried `outputFileTracingIncludes` with route pattern approach — failed due to wrong pattern syntax
- Currently fixing the pattern and adding a `copyFiles` fallback approach
- Just committed and pushed changes via GitHub API, then triggered a Vercel rebuild

## What to Pick Up Next
1. Verify the Vercel rebuild completes successfully
2. Check if JSON files are now accessible in the serverless function (test a property page that should have AI content)
3. If still failing, investigate whether the JSON path in the function is correct or if file-system access is blocked

## Key Files
- `next.config.js` / `next.config.ts` — `outputFileTracingIncludes` setting
- Route handlers that load the AI content JSON
