# Carry-Forward Brief: hanssonhertzell.se Next.js Migration
**Session:** local_691c0ff0-5f2d-4323-90fa-531b5fcbd71e  
**Status:** Running (169+ assistant turns)  
**Updated:** 2026-06-03T00:05:22

## What's happening
Migrating hanssonhertzell.se to Next.js and deploying on Netlify. Currently debugging a Swedish locale routing issue.

## Latest activity
Diagnosing a double-hop redirect for `/sv/` URLs. The `_redirects` file in GitHub is correct — `/sv/news/` rules at line 45, `/sv/*:splat` at line 239 — but the `@netlify/plugin-nextjs` is generating its own additional locale-stripping rule for `sv` (the default locale), causing a double redirect. Checking if CDN cache has caught up after a recent fix.

## Key technical context
- Default locale is `sv` (Swedish) in Next.js i18n config
- Netlify plugin auto-generates locale-strip redirects for default locale paths
- `_redirects` has manual `/sv/*` rules that may conflict with plugin-generated rules
- Issue: plugin rule fires first → strips `/sv/` → hits another redirect rule → double-hop

## Next likely steps
- Verify CDN has flushed stale rules
- May need to disable automatic locale detection in `next.config.js` or adjust plugin settings
- Test `/sv/news/` and other Swedish-locale paths after cache purge
