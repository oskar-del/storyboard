{
  "_instructions": [
    "1. Open Terminal and run: open ~/Library/Application\\ Support/Claude/claude_desktop_config.json",
    "2. Add the 'storyboard' entry inside your existing 'mcpServers' object",
    "3. Save the file, then restart Claude Desktop (Cmd+Q and reopen)",
    "4. You'll see 'storyboard' appear in the MCP servers panel"
  ],

  "mcpServers": {
    "storyboard": {
      "command": "node",
      "args": ["/Users/oskarpeterson/Documents/AI/AGENCY /storyboard/mcp-server.js"]
    }
  }
}
